const SecWallet = require('./indexWallet')
const SecHDKey = require('./indexHd')
const ThiredParty = require('./thirdparty')

module.exports = {
  SecWallet: SecWallet,
  SecHDKey: SecHDKey,
  ThiredParty: ThiredParty
}
